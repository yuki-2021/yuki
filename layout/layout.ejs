<!DOCTYPE html>
<html>
    <%- partial('_partial/head')%>
    <body>
        <!-- 顶部导航 -->
        <%if(page.path == 'index.html'){%>
            <%- partial('_partial/header',{headerOpt: 'yuki-header-trans'})%>
        <%}%>
        <%if(page.path != 'index.html'){%>
            <%- partial('_partial/header',{headerOpt: 'yuki-header-no-trans'})%>
        <%}%>
        
        <!-- jumbotron -->
        
        <!-- index页面 -->
        <main class="main">
            <%- body%>
        </main>

        <!-- 底部fotter -->
        <%- partial('_partial/foot')%>
    </body>
</html>